# BhashaLLM Directory Structure

**This file has moved to [`docs/DIRECTORY_STRUCTURE.md`](docs/DIRECTORY_STRUCTURE.md).**

`APPLY_THIS_PATCH.md` step 3 asks for it to live under `docs/` with the
"Note for AI Assistants" section removed. The moved copy is current and
covers the directories added since — `configs/`, `eval/`, `benchmarks/`,
`human_eval/`, `scripts/` — and preserves the four refactoring conventions
from the old note under a **Conventions** heading.

This stub is left in place so existing links do not break. It can be
deleted once nothing points at it.
