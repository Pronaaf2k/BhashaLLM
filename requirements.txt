# Direct dependencies of the BhashaLLM pipeline.
# The complete frozen development environment is in requirements-full.lock.
# Versions match docs/environment_capture.txt; regenerate that file rather
# than editing versions here by hand.

# --- core model stack -------------------------------------------------
torch==2.10.0
torchvision==0.25.0
transformers==4.57.6
peft==0.18.1
bitsandbytes==0.49.1
accelerate==1.12.0
trl==0.28.0
datasets==4.5.0
tokenizers==0.22.2
sentencepiece==0.2.1
safetensors==0.7.0
huggingface_hub==0.36.2

# --- vision / OCR -----------------------------------------------------
qwen-vl-utils==0.0.14
pillow==12.1.0

# --- serving ----------------------------------------------------------
fastapi==0.128.7
uvicorn==0.40.0
pydantic==2.12.5

# --- evaluation -------------------------------------------------------
# jiwer was already pinned and supports CER; eval/ocr_cer.py implements
# alignment directly so it is optional.
jiwer==4.0.0
# Not previously present. Table VI (ROUGE) and the BLEU/chrF++ figures in
# Sec. V-A could not be regenerated without these. See docs/ERRATA.md B8.
sacrebleu>=2.4.0

# --- utilities --------------------------------------------------------
numpy==2.4.2
pandas==3.0.0
scikit-learn==1.8.0
PyYAML==6.0.2
tqdm==4.67.3

# NOT dependencies of this project, despite appearing in the previous
# requirements.txt: chromadb, chroma-hnswlib, langchain*, sentence-transformers
# (a retrieval stack, while Sec. III-D explains that retrieval was not used),
# paddleocr, paddlepaddle, paddlepaddle-gpu, paddlex, pytesseract (three OCR
# engines the methodology does not mention), agentmail, posthog, kubernetes,
# modelscope. All retained in requirements-full.lock. See docs/ERRATA.md C.
